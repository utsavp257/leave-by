.venv/
__pycache__/
*.pyc
.pytest_cache/

# Cached pulls from TLC and the state open data portal. Rebuilt on demand and
# far too big to track. Anchored to the root on purpose: an unanchored `data/`
# would also swallow site/data, which is exactly what does need committing.
/data/
